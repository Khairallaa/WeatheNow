import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weathernow.app',
  appName: 'WeatherNow',
  webDir: 'www.index.html'
};

export default config;
